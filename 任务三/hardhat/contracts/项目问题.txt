1.--force版本问题解决
2.IERC721(nftContract).safeTransferFrom(address(this), highestBidder, tokenId);需要配合ReentrancyGuard使用？
3. await  await upgrades.deployProxy(CustomNFT, [
    "AuctionMarketNFT", 
    "AMNFT"
  ], {
    initializer: "initialize",
    kind: "transparent"
  });
  和await Auction.deploy();的区别